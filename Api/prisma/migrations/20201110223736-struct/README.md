# Migration `20201110223736-struct`

This migration has been generated by William Santos at 11/10/2020, 7:37:36 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Usuario` (
`id` int  NOT NULL  AUTO_INCREMENT,
`email` varchar(191)  NOT NULL ,
`password` varchar(191)  NOT NULL ,
`gruposId` int  ,
UNIQUE INDEX `Usuario.email_unique`(`email`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Mensagens` (
`id` int  NOT NULL  AUTO_INCREMENT,
`gruposId` int  ,
`usuarioId` int  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Grupos` (
`id` int  NOT NULL  AUTO_INCREMENT,
`name` varchar(191)  NOT NULL ,
UNIQUE INDEX `Grupos.name_unique`(`name`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Usuario` ADD FOREIGN KEY (`gruposId`) REFERENCES `Grupos`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `Mensagens` ADD FOREIGN KEY (`gruposId`) REFERENCES `Grupos`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `Mensagens` ADD FOREIGN KEY (`usuarioId`) REFERENCES `Usuario`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201110223736-struct
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Usuario {
+  id        Int         @id @default(autoincrement())
+  email     String      @unique
+  password  String
+  Grupos    Grupos?     @relation(fields: [gruposId], references: [id])
+  gruposId  Int?
+  Mensagens Mensagens[]
+}
+
+model Mensagens {
+  id        Int     @id @default(autoincrement())
+  Grupos    Grupos? @relation(fields: [gruposId], references: [id])
+  gruposId  Int?
+  Usuario   Usuario @relation(fields: [usuarioId], references: [id])
+  usuarioId Int
+}
+
+model Grupos {
+  id        Int         @id @default(autoincrement())
+  name      String      @unique
+  Usuarios  Usuario[]
+  Mensagens Mensagens[]
+}
```


